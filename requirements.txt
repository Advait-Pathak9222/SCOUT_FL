# SCOUT-FL / JEDI-FL — runtime dependencies
# On the GPU laptop install the CUDA build of torch FIRST, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# then:  pip install -r requirements.txt
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
matplotlib>=3.7
torch>=2.0
torchvision>=0.15
pytest>=7.0          # tests only; not needed to run the experiments
